version: '3'

services:
  web:
    build: .
    ports:
      - "${PORT}:5000"
    environment:
      - NODE_ENV=development
      - JWT_SECRET=${JWT_SECRET}
      - URL=${URL}
      - MONGO_URI=${MONGO_URI}