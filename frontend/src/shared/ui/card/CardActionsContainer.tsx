import Box from '@mui/material/Box';
import styled from '@mui/system/styled';

export const CardActionsContainer = styled(Box)(() => ({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
}));
